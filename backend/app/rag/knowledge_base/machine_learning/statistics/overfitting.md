# Overfitting

Overfitting happens when a model learns noise or dataset-specific patterns that do not generalize.
Complex models such as boosted trees can overfit small datasets if hyperparameters are not controlled.

Validation metrics, train/test splits, cross validation, and regularization help detect and reduce overfitting.
