# Logistic Regression

Logistic Regression is a linear classification model that estimates class probabilities.
It often performs well on tabular datasets when the engineered features are clean, scaled, and not overly nonlinear.

It can outperform complex models when the dataset is small, the signal is mostly linear, or the complex models overfit.
Regularization helps control overfitting and improves generalization.
